# Existing Model Preflight: WorldGuard SkillGuard 0.4 Maintenance

## Task

Refresh WorldGuard author maintenance without changing WorldGuard runtime behavior, adding SkillGuard domain depth, activating a global installation, or retaining duplicate FlowGuard author authority.

## Model search

- Canonical plane: `development_process`; WorldGuard product and agent-operation semantics remain target-owned related context.
- Search paths: `.flowguard/*.py`, `.flowguard/*.md`, `skills/worldguard/.skillguard/contract-source.json`, `skills/worldguard/.skillguard/contract_model.py`, and OpenSpec change `refresh-skillguard-0-4-maintenance`.
- Primary hits:
  - `worldguard-skillguard-declared-checks-current`
  - `worldguard-claim-derived-semantic-coverage`
  - `worldguard-guard-model-failure-exhaustion`
  - WorldGuard template-pack selection/build model
  - existing WorldGuard model-mesh, field-lifecycle, and development-process records
- OpenSpec context is planning authority only. It is not a model, test owner, native receipt, or product-runtime authority.

## Existing ownership

- FunctionBlocks: the target contract declares five checks for contract export, Guard-model semantics, predictive native depth, template packs, and internal-Guard topology.
- State: `unit:worldguard` owns author-maintenance state; WorldGuard owns its model, mesh, templates, seven Guard routes, and domain judgments.
- Side effects: the current SkillGuard compiler owns only `compiled-contract.json` and `check-manifest.json`; validation owns only the fresh private run and owner-evidence roots.
- Public entrypoint: `skills/worldguard/SKILL.md`; the Python `worldguard` package and bundled runtime are one exact-parity executable identity.
- Project tool surface: the current global clean FlowGuard consumer, as declared by the 0.59.0 project record.

## Reuse decision

`extend_existing`: reuse the target contract export and existing WorldGuard coverage, Guard-contract, template, mesh, field, and process models. Add one bounded maintenance-process model that derives StructureMesh and TestMesh records from those owners. No product or target-domain boundary is introduced.

## Duplicate and dependency evidence

- The proposed obsolete surface contained 137 tracked paths under `.agents/skills/**` plus `.skillguard/flowguard-suite/suite-map.json`, totaling 1,598,329 bytes before removal.
- A tracked-content search outside those paths found no executable, test, packaging, or public-entrypoint dependency. The only current tracked reference is the generated `AGENTS.md` rule explicitly stating that an ordinary target project does not copy the FlowGuard suite locally or own its suite map.
- `.agents/skills` is a plain directory, not a link or external mount.
- The global FlowGuard consumer contains its own `consumer-release.json`, and the current FlowGuard project audit passes against that global projection.
- Therefore the local copies and suite map are `remove` candidates, not compatibility or fallback authorities. Any newly discovered live dependency reclassifies the removal as blocked.

## Runtime parity evidence

- The canonical and bundled fuel-cell YAML parse to the same line content and Git-normalized blob.
- Their working-tree bytes differ only in mixed newline placement before normalization, so exact target topology validation is correctly blocked until one byte representation is restored.

## Downstream routes

- `flowguard-structure-mesh`: singular public skill/runtime authority and removal of duplicate FlowGuard author copies.
- `flowguard-test-mesh`: exactly five target-declared checks with one owner and one subject each.
- `flowguard-development-process-flow`: OpenSpec, preflight, compile, reduction, validation, projection, and local-closure order plus freshness.
- `skillguard`: current author adoption, direct generated-authority replacement, same-unit execution, evidence audit, and clean projection comparison.

## Claim boundary

This preflight selects and extends existing owners and proves removal eligibility from current repository references. It does not itself prove byte normalization, generated parity, check execution, consumer installation, publication, release, or future AI behavior.
