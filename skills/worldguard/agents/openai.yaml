interface:
  display_name: "WorldGuard"
  short_description: "One entry for seven complete world-model Guards"
  default_prompt: "Use $worldguard as the sole entry. Extract typed task facts with source spans, select exactly one public task shape, then derive the complete one-or-more internal Guard set from structured claim semantics. Read entry-routing.md and only the references required by the selected shape/Guards. Load task-local-model-deepening.md for prediction or current predictive gaps. Preserve exact native terminals and never use a child skill, caller subset, keyword score, understanding level, alias, retry, or fallback as authority."
