## 1. Route authority and entry shell

- [x] 1.1 Add exact public task-shape admission and extend the existing seven-route registry with complete claim-derived semantics, first-action, reference, deep-trigger, and claim-boundary capsules.
- [x] 1.2 Add concise entry-routing and task-local-deepening references that point to existing detailed contracts without duplicating authority.
- [x] 1.3 Rewrite `SKILL.md` and `agents/openai.yaml` as a thin exact-one task-shape shell with complete claim-derived Guard coverage and conditional reference loading.

## 2. Executable prompt and topology checks

- [x] 2.1 Upgrade the internal topology checker to validate complete capsules, resolvable references, and runtime owner parity.
- [x] 2.2 Add target-owned prompt-bundle/load-graph fixtures for bounded, predictive multi-Guard, mesh, template, no-match, unmapped, shape ambiguity, forbidden, and missing-reference cases.
- [x] 2.3 Add prompt budgets and mandatory reasoning headroom without relaxing required semantic fields.

## 3. Governance and identity

- [x] 3.1 Update and compile the WorldGuard SkillGuard contract, model inputs, and exact affected-check ownership.
- [x] 3.2 Remove active-change-path authority from the modeled inputs and bind the implementation handoff to the canonical main specs; renew FlowGuard model authority only in the parent integration phase after this change is archived and the source is frozen.
- [x] 3.3 Update source, bundled runtime, README, changelog, and package metadata to patch version `0.7.1`.

## 4. Validation and publication

- [x] 4.1 Run affected native, prompt, topology, OpenSpec, package, and model checks; fix all failures.
- [x] 4.2 Hand the archived source snapshot to the parent integration phase for one frozen full SkillGuard validation; do not claim that gate from affected-only change evidence.
- [x] 4.3 Hand transactional consumer installation and installed-currentness verification to the parent integration phase, keeping source and install identities separate.
- [x] 4.4 Complete local README/release/privacy preparation and hand commit, push, tag `v0.7.1`, GitHub Release publication, and exact remote identity verification to the parent integration phase after archive.
