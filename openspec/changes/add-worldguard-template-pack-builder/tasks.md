## 1. Model And Governance Baseline

- [x] 1.1 Add and run a full ExistingModelPreflight artifact that reuses the current GuardContract, Guard purpose-proof, and ModelMesh owners and rejects parallel semantic authority.
- [x] 1.2 Add and run the real FlowGuard template selection/composition/validation model with zero, one, many, stale, conflict, and validator-failure cases.
- [x] 1.3 Record template-pack field lifecycle ownership and the DevelopmentProcessFlow/SpecWorkPackage validation-owner mapping, including output-only task/report invalidation boundaries.

## 2. WorldGuard Template-Pack Runtime

- [x] 2.1 Implement versioned template fragments and manifests with exact leaf-field ownership and content fingerprints.
- [x] 2.2 Implement deterministic base/selected/ambiguous selection and explicit no-base blocking.
- [x] 2.3 Implement disjoint composition, exact slot resolution, and typed conflict/missing/unused errors.
- [x] 2.4 Implement WorldGuard-native validator bindings, construction receipts, and exact instance fingerprints without duplicating Guard semantics.
- [x] 2.5 Add built-in GuardContract and ModelMeshContract base/candidate packs and export the public builder API.

## 3. Counterexample And Parity Coverage

- [x] 3.1 Add positive GuardContract and ModelMeshContract build tests with stable selection and instance fingerprints.
- [x] 3.2 Add no-match/base, multiple-match ambiguity, field conflict, stale manifest, slot error, and unknown-validator counterexample tests.
- [x] 3.3 Mirror the WorldGuard-owned module into the bundled SkillGuard runtime and prove exact source/bundle parity.

## 4. Skill And Contract Integration

- [x] 4.1 Update the WorldGuard skill and template-pack reference so future AI uses the builder before from-scratch construction while preserving native semantic authority.
- [x] 4.2 Update the current SkillGuard contract-source input/check binding and compile the derived compiled-contract/check-manifest authority.

## 5. Frozen Validation And Handoff

- [x] 5.1 Run and retain the focused FlowGuard, template-pack pytest, Guard-model contract, and SkillGuard contract checks under their frozen owners.
- [x] 5.2 Freeze the stable source identity and run exactly one final full pytest owner; treat tasks, reports, logs, and receipts as output-only.
- [x] 5.3 Run OpenSpec verification, write the evidence-backed verification report, and confirm the change is apply-ready with every task accounted for.

## 6. Target-Owned Neutral SkillGuard Projection

- [x] 6.1 Extend the ExistingModelPreflight, executable FlowGuard model, field lifecycle, and DevelopmentProcessFlow records for a WorldGuard-owned neutral projection boundary.
- [x] 6.2 Implement the exact unsealed `skillguard.target_template_projection.v1` and `skillguard.template_catalog.v1` adapter from current WorldGuard manifests, native selection/applicability, and content identities.
- [x] 6.3 Add focused good, unknown-root-field, native candidate-equality, wrong-route, and stale-native-identity regression tests.
- [x] 6.4 Mirror the adapter into the bundled runtime and update the existing WorldGuard template-pack native check, reference, and skill guidance without transferring semantics to SkillGuard.
- [x] 6.5 Update and compile the already-owned SkillGuard native check/contract trio, then run only the focused and affected validation owners plus a read-only central projection compile.
- [x] 6.6 Update the scoped verification report and run strict OpenSpec validation without executing a second full-repository test owner.
