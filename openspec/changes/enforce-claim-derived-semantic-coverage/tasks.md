## 1. Claim And Coverage Contracts

- [x] 1.1 Add backward-compatible structured claim atoms and WorldGuard-owned semantic-to-Guard route derivation.
- [x] 1.2 Add a mesh semantic-coverage contract for expected nodes, scenarios, holdouts, horizon, branches, perturbations, and profile.
- [x] 1.3 Extend the native depth receipt with quantitative expected/executed/skipped coverage, predictive gaps, fingerprints, and predictive licensing.
- [x] 1.4 Add target-owned representative count/ratio floors and non-replaceable native early/middle/late strata.
- [x] 1.5 Add per-model-node predictive depth so aggregate evidence cannot hide a shallow object.
- [x] 1.6 Discover and reconcile every semantic/predictive mesh node; make expected ids a completeness assertion and require closed non-contributing exclusions.
- [x] 1.7 Add per-variable/signal temporal child universes when a model exposes those objects.
- [x] 1.8 Emit WorldGuard-owned per-object native dynamic-floor receipts and remove the duplicate fixed one-percent SkillGuard floor.
- [x] 1.9 Strengthen the native temporal policy from logarithmic to square-root growth, preserve exact selected timepoint identities, and add a maximum-normalized-gap gate so clustered samples cannot pass from count and phase labels alone.
- [x] 1.10 Emit exact content-addressed native observations for semantic children, model-node and variable timepoint depth, scenario/holdout portfolio, predictive axes, native floor policy, and claim routes.

## 2. Native Semantic Execution

- [x] 2.1 Fail closed when a claim-derived Guard or expected contract-bearing node is missing, and preserve typed skipped-child evidence.
- [x] 2.2 Extend bounded event/causal executors with safe native rollout, intervention, counterfactual, and holdout evidence.
- [x] 2.3 Keep local semantic PASS distinct from predictive readiness and project incomplete requested prediction to aggregate GAP/BOUNDARY.

## 3. Models, Tests, And Skills

- [x] 3.1 Add shallow single-event/single-equation, omitted-route, contractless-node, and complete predictive fixture tests.
- [x] 3.2 Add the claim-derived-coverage FlowGuard process model/runner and bind modeled obligations to focused tests.
- [x] 3.3 Update WorldGuard skill/references and target-local SkillGuard bindings so per-target receipts remain required and static regressions are engine health only.
- [x] 3.4 Add two-of-1,000, one-phase, caller-strata-bypass, and aggregate-rich/object-shallow negative fixtures.
- [x] 3.5 Add per-required-Guard scenario/holdout depth plus the portable SkillGuard V2 contract source, compiled contract, exact check manifest, dynamic target-native inventories, real object classes, and content-addressed positive/shallow calibration while preserving all existing WorldGuard routes.
- [x] 3.6 Extend FlowGuard, SkillGuard object-scope attestation, V2/V3 contract source, and skill references for node reconciliation, exclusions, native floors, and variable/signal child universes.
- [x] 3.7 Remove synthetic transport-health evidence and model native-observation integrity as a hard gate; preserve empty shallow result sets exactly.
- [x] 3.8 Add 32-of-1,000 and 100-of-10,000 native-floor regressions plus a count-complete/all-phases-but-large-hole negative case, and update the public skill/reference boundary.
- [x] 3.9 Declare seven target-owned Guard prevention purposes and boundaries, one native good per Guard, and a source-discovered finite Guard-owned failure inventory.
- [x] 3.10 Materialize exactly one native bad per Guard-owned failure class, add exact status/code oracles, and bind the 43-case inventory to a child ContractExhaustionMesh under the existing FlowGuard owner.
- [x] 3.11 Fix CausalGuard partial-equation false PASS and add source/runtime parity plus focused regression evidence.
- [x] 3.12 Directly replace the target SkillGuard contract with the current generic schema, `native-integrated`, exactly one `enforced` profile, and target-native Guard-model/depth checks without optional modes or SkillGuard-owned domain semantics.
- [x] 3.13 Retire the target-native `closure_profile` field and CLI selector so every current mesh runs required semantics and legacy structural-only input fails closed.
- [x] 3.14 Expose the existing WorldGuard owner, default route, complete native route bindings, and required native check bindings at the current contract top level.

## 4. Verification

- [x] 4.1 Run focused claim-derived/semantic tests and fix every failure.
- [x] 4.2 Run the FlowGuard runner, strict OpenSpec validation, project audits, and full WorldGuard regression.
- [x] 4.3 Run OpenSpec verification, confirm all required checks are current, and leave the change unarchived and uncommitted.
- [x] 4.4 Run separated-root SkillGuard V2 supervised deep, two-point, and one-phase smoke, confirm only deep closure, then refresh the final verification report.
- [x] 4.5 Run focused omitted-node, unresolved-exclusion, excluded-contribution, per-variable shallow, and native-floor parity regressions plus current FlowGuard/OpenSpec verification.
- [x] 4.6 After the shared SkillGuard identity protocol freezes, replace mechanical contribution spans with exact native observation locators and rerun the single owned final verification.
- [x] 4.7 Remove the expanded set of unreferenced V1 generic checkers, policies, mutable reports/evidence/ledgers, and invalidate the former narrow completion receipt.
- [x] 4.8 Prove source/install residual absence and installation-currentness replay, then issue a new expanded-scope retirement completion receipt.
- [x] 4.9 Replace command-driven OpenSpec verification with a fail-closed consumer of the single WorldGuard parent receipt.
- [x] 4.10 Prove the scheduled identity is carried by the exact target-owned mesh and an identity supplied only through generic supervisor request metadata is blocked.
- [x] 4.11 Bundle and fingerprint the complete native WorldGuard runtime inside the installed skill, require it in every applicable target-native declared-check input set, and reject external/global import authority for formal closure.
- [x] 4.12 Run the target-native Guard-model oracle, exact 43-case ContractExhaustion review, focused pytest regression, existing FlowGuard owner, strict OpenSpec validation, current SkillGuard authority, and deterministic contract parity checks. Installation, archive, and parent full aggregation remain explicitly not run here.
- [x] 4.13 Recompile current authority and run the affected no-mode, router-binding, Guard-model, semantic-rollout, parity, FlowGuard, and strict OpenSpec checks. Leave installation and the single final parent owner to the frozen portfolio stage.
