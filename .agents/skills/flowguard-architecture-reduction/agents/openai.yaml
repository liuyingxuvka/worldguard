interface:
  display_name: "FlowGuard Architecture Reduction"
  short_description: "Prove behavior-preserving architecture contraction"
  default_prompt: "Use $flowguard-architecture-reduction for route_id=architecture_reduction, role=public_owner, native_owner=architecture_reduction; require an observable contract, independently expected same-intent candidate inventory, materialized similarity relation/code obligations, code mapping, stable intent/commitment/selected-path binding, proof status, and target handoff; empty or omitted duplicate candidates block; a retained facade needs current selected-path delegation evidence and no independent success; do not refactor production code or invent equivalence; output evidence, failures, blockers, skipped_checks, residual_risk, claim_boundary, and typed_next_actions; claim only current reduction-candidate readiness. When creating or materially changing a concrete model instance, first freeze task/instance-specific one-or-many protected failures and the claim boundary, then bind the exact candidate plus native good, per-failure bad, oracle, and current evidence; reusable model types are not permanently single-purpose; FlowGuard owns semantics and SkillGuard only supervises declared checks."
